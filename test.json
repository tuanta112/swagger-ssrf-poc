{
  "swagger": "2.0",
  "info": {
    "title": "SSRF Scan & LFD Test",
    "description": "This is a fake API designed to trigger internal SSRF and Local File Disclosure vulnerabilities.",
    "version": "1.0.0"
  },
  "host": "127.0.0.1:80",
  "basePath": "/",
  "schemes": ["http"],
  "paths": {
    "/proxy": {
      "get": {
        "summary": "SSRF Proxy Endpoint",
        "description": "Pass any URL to be fetched by the server (potential SSRF).",
        "parameters": [
          {
            "name": "url",
            "in": "query",
            "description": "Target URL to fetch (can be file:// or http://internal-ip)",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "200": {
            "description": "Content of remote or local file"
          }
        }
      }
    },
    "/readfile": {
      "get": {
        "summary": "Read local file (if SSRF supports file://)",
        "description": "Attempts to read /etc/passwd or other local files via SSRF.",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "description": "Local file path to read (e.g., file:///etc/passwd)",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "200": {
            "description": "Local file content"
          }
        }
      }
    },
    "/scan": {
      "get": {
        "summary": "Internal Host Scanner",
        "description": "Attempts to connect to internal IP ranges via SSRF.",
        "parameters": [
          {
            "name": "ip",
            "in": "query",
            "description": "Internal IP to scan (e.g. http://192.168.0.1:80)",
            "required": true,
            "type": "string"
          }
        ],
        "responses": {
          "200": {
            "description": "Scan result (open port or response)"
          }
        }
      }
    }
  }
}
